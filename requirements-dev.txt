# Strumenti di sviluppo: test, lint, type-check. Non necessari per far girare l'app.
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0
ruff>=0.6
mypy>=1.11
# Nota: 'pandas-stubs' NON è incluso di proposito. Le sue release seguono pandas
# 2.x, mentre qui usiamo pandas 3.0: gli stub disallineati producevano decine di
# falsi positivi in mypy. Finché non esiste una versione per pandas 3.x, pandas
# resta senza stub (type-check comunque attivo sul resto del codice).
