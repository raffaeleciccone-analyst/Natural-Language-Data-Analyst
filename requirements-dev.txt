# Strumenti di sviluppo: test, lint, type-check. Non necessari per far girare l'app.
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0
ruff>=0.6
mypy>=1.11
pandas-stubs>=2.0     # stub di tipo per pandas (mypy)
