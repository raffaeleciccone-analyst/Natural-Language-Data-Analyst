# === Manifest di DEPLOY (Streamlit Community Cloud) ===
#
# Streamlit Cloud installa esclusivamente da questo file, quindi non può essere
# sostituito da pyproject.toml. Contiene il minimo necessario alla demo pubblica:
# le dipendenze dell'app più il solo SDK del provider che il deploy usa davvero
# (Groq, che parla l'API OpenAI — vedi DEPLOY.md).
#
# Per sviluppare in locale NON serve questo file: la fonte di verità è
# pyproject.toml, che permette anche di scegliere i provider.
#     pip install -e ".[all,dev]"     tutti i provider + strumenti di sviluppo
#     pip install -e ".[ollama]"      solo Ollama (locale, senza API key)
#
# Se cambi provider alla demo, sostituisci la riga dell'SDK qui sotto:
#   anthropic -> anthropic>=0.40,<1.0
#   gemini    -> google-genai>=0.3,<3.0

streamlit==1.60.0
pandas==3.0.5
numpy==2.5.1          # pinnata: la sua versione determina gli stub di tipo usati in CI
plotly==7.0.0
openpyxl==3.1.5

openai>=1.40,<3.0     # provider della demo (Groq via API compatibile OpenAI)
